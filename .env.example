# Optional (recommended): bootstrap/update the admin account on startup
SUPERBOWL_ADMIN_USERNAME=tejas
SUPERBOWL_ADMIN_PASSWORD=change-me
SUPERBOWL_ADMIN_DISPLAY_NAME=Tejas

# Optional: custom SQLite path (helpful on some hosts)
# SUPERBOWL_SQUARES_DB_PATH=./data/squares.db

# Optional: use Postgres/Neon instead of SQLite
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
